{
  "name": "localcraft",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["frontend", "backend", "shared"],
  "scripts": {
    "dev": "npm run build -w shared && concurrently -n backend,frontend -c cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "test": "npm run build -w shared && npm run test -w shared && npm run test -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
